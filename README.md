A real-time peer-to-peer video calling application built with WebRTC.
Uses Node.js and Express as the backend server, Socket.IO for WebRTC signaling (exchanging SDP offers, answers, and ICE candidates between peers), and Twilio Network Traversal Service to generate TURN server credentials for reliable connectivity across different networks and NAT configurations.
